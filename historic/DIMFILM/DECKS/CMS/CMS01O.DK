*DECK CMS01O F
      SUBROUTINE DFX01O(DN,ODN,BYTES)
      INTEGER DN,ODN,BYTES
      INTEGER*2 BUFF(120)
C    BUFF MUST EXCEED LENGTH DECLARED IN MAIN DRIVERS
      REWIND DN
    1 READ (DN,END=2) NW
      READ (DN,END=2) (BUFF(I),I=1,NW)
      WRITE(ODN) (BUFF(I),I=1,NW)
      BYTES = BYTES + 2*NW
      GOTO 1
    2 RETURN
      END
